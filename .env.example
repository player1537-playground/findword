# Django Secret Key (REQUIRED - generate a secure random string for production)
# You can generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-placeholder-change-this-in-production

# Debug mode (default: False in production)
# Set to True only for local development
DEBUG=False

# Allowed hosts (comma-separated, no spaces)
# For local development: ALLOWED_HOSTS=localhost,127.0.0.1
# For production: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Additional production settings to consider:
# - Use a proper SECRET_KEY (see above)
# - Set DEBUG=False in production
# - Configure ALLOWED_HOSTS for your domain
# - Consider using environment-specific .env files (.env.local, .env.production)
